name: parksy_melody
description: Parksy Melody — YouTube audio cutter with preset clips & Telegram bridge
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  youtube_explode_dart: ^2.3.0
  just_audio: ^0.9.36
  ffmpeg_kit_flutter_new_audio: ^2.0.0
  path_provider: ^2.1.1
  http: ^1.2.0
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
